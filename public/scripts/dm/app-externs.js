/* App variables exposed by server declared for clsoure compiler when building
 * dist script. */

/**
 * @type {Object.<string,*>}
 */

var dmAssets;

/**
 * @type {Object.<string, Object>}
 */
dmAssets.dbs;