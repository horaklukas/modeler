table = require '../src/objects/table'

describe 'class Table', ->
	#tb = new table RaphaelMock, 0, 0

	it 'constructor should create rectangle', ->
		#assert.isNotNull tb.obj, 'rectange not created'

	# have width 80 and height 60 when not passed to constructor', ->
		